#include "sprite.h"

// this isn't ment to be finished yet.
// just a placeholder.
namespace core{
	namespace gfx{
		Sprite::Sprite( float x, float y, float w, float h )
		: x(x), y(y), w(w), h(h){
		}
		Sprite::~Sprite(){
		}
	};
};
